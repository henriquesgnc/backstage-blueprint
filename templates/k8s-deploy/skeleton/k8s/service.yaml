apiVersion: v1
kind: Service
metadata:
  name: ${{ values.appName }}
  labels:
    app: ${{ values.appName }}
spec:
  type: ClusterIP
  selector:
    app: ${{ values.appName }}
  ports:
    - port: 80
      targetPort: ${{ values.port }}
      protocol: TCP
      name: http
