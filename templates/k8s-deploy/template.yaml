apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: k8s-deploy
  title: Deploy to Kubernetes
  description: Scaffold an application with Kubernetes manifests and CI/CD pipeline
  tags:
    - kubernetes
    - k8s
    - deploy
    - docker
spec:
  owner: user:guest
  type: service

  parameters:
    - title: Application Details
      required:
        - appName
        - githubOrg
      properties:
        githubOrg:
          title: GitHub Organization
          type: string
          description: GitHub organization or user where the repo will be created
          default: replace-with-your-github-org
          ui:help: "Example: my-company, henriquesgnc"
        appName:
          title: Application Name
          type: string
          description: Name of the application (used for K8s resources and repo)
          pattern: "^[a-z0-9-]+$"
          ui:autofocus: true
          ui:help: "Lowercase, no spaces. Example: my-api"
        port:
          title: Container Port
          type: integer
          description: Port the application listens on
          default: 3000
        replicas:
          title: Replicas
          type: integer
          description: Number of pod replicas
          default: 2
          minimum: 1
          maximum: 10
        registry:
          title: Container Registry
          type: string
          default: ecr
          enum:
            - ecr
            - dockerhub
          enumNames:
            - AWS ECR
            - Docker Hub

  steps:
    - id: fetch
      name: Fetch Skeleton
      action: fetch:template
      input:
        url: ./skeleton
        values:
          appName: ${{ parameters.appName }}
          port: ${{ parameters.port }}
          replicas: ${{ parameters.replicas }}
          registry: ${{ parameters.registry }}
          githubOrg: ${{ parameters.githubOrg }}

    - id: publish
      name: Publish to GitHub
      action: publish:github
      input:
        repoUrl: github.com?repo=${{ parameters.appName }}&owner=${{ parameters.githubOrg }}
        defaultBranch: main
        allowedHosts: ["github.com"]

    - id: register
      name: Register in Catalog
      action: catalog:register
      input:
        repoContentsUrl: ${{ steps.publish.output.repoContentsUrl }}
        catalogInfoPath: catalog-info.yaml

  output:
    links:
      - title: Open Repository
        url: ${{ steps.publish.output.remoteUrl }}
        icon: github
